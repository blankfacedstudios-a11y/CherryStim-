# AGENTS.md

## Cursor Cloud specific instructions

This repository (`CherryStim-`) is currently an empty skeleton with only a `README.md`.
No source code, dependencies, build system, or services exist yet.

- **No dependencies to install** — no `package.json`, `requirements.txt`, or similar files are present.
- **No services to run** — there are no applications, servers, or background processes.
- **No lint, test, or build commands** — none are configured.

When source code and a build system are added, update this section with:
1. How to install dependencies (package manager and command).
2. How to run lint, test, and build.
3. How to start the development server(s).
4. Any non-obvious environment caveats discovered during setup.
